# Subset
# Explanation 
In this code we print all the subsets of array 
without duplicates, using recursion to explore
choice at each index. This is a classic backtracking 
problem where for each element, you decide 
to include it or to exclude it
