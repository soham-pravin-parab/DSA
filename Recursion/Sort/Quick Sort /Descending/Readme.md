# QuickSort (Descending)
# Explanation 
QuickSort in this code is based on the divide and conquer 
algorithm this recursively sorts the vector 
in descending order by selecting the last element as pivot
and partitioning the array around it placing elements 
greater than or equal to on the the left
