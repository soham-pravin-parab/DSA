# Merge Sort
# Explanation 
Merge sort is a classic divide and conquer 
algorithm with O(n logn) time complexity 
that works recursively dividing the array 
into halves, sorting them,and then merging
them back in the order
