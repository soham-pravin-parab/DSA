# Rat Maze (optimised)
# Explanation 

The optimised version saves O(n²) space by reusing the maze matrix, ideal for large mazes. Both explore the same recursion tree (4 directions: D,U,L,R) and backtrack correctly.The optimised version saves O(n²) space by reusing the maze matrix, ideal for large mazes. Both explore the same recursion tree (4 directions: D,U,L,R) and backtrack correctly.

