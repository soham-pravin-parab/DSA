# Search 
# Explanation 
The int search(int key) function performs linear search starting at head with index 0. It loops through nodes: if temp->data matches key, returns the index; otherwise advances temp and increments index. Returns -1 if not found (O(n) worst-case).
