# Doubly List PUSH_BACK 
# Explanation 
This code allocates a new node with data
set to val , next and previous implicitly Null
if head is null sets head = tail = newNode
making it the only node. if the head doesn't equal to null then it links new node
backward to current tail links current tail 
forward  to new node updates tail to
the new end .
